{
  "name": "obsidian-gym-tracker",
  "version": "1.0.21",
  "private": true,
  "description": "Gym workout tracker for Obsidian — calendar view, templates, and quick workout logging",
  "main": "main.js",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production"
  },
  "keywords": ["obsidian", "plugin", "gym", "workout", "fitness"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.0.0",
    "esbuild": "^0.20.0",
    "obsidian": "^1.7.0",
    "typescript": "^5.0.0"
  }
}
